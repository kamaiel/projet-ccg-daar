import { SetStateAction, useEffect, useMemo, useRef, useState } from 'react'
import "./Accueil.css"



export const Accueil = ({ wallet }) => {
    
    return (
        <div>
            <h1>Accueil</h1>
        </div>
    );
};
